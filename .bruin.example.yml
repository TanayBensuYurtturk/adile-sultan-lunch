# Copy this file to `.bruin.yml` (which is git-ignored) and fill in your own paths.
# `.bruin.yml` holds credentials, so it must never be committed.
default_environment: default
environments:
    default:
        connections:
            # BigQuery — destination for the scraper and home of the bot-managed lunch.votes table.
            google_cloud_platform:
                - name: gcp-default
                  project_id: id
                  service_account_file: key.json
