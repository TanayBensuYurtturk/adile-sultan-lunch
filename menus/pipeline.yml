name: adile-sultan-menus
# Runs every morning at 09:00 to snapshot the day's menus before the team votes.
schedule: "0 9 * * *"
start_date: "2026-07-07"
catchup: false

default_connections:
    google_cloud_platform: gcp-default
